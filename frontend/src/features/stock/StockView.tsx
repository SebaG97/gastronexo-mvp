import { Warehouse } from 'lucide-react'
import { PlaceholderModule } from '../shared/PlaceholderModule'

export function StockView() {
  return <PlaceholderModule icon={Warehouse} title="Stock" description="Controlá existencias, lotes y puntos de reposición." itemLabel="movimientos" />
}
